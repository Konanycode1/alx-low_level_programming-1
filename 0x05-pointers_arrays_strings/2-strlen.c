#include "main.h"
#include <string.h>
/**
*_strlen - Write a function that returns the length of a string
*@s: is a charactere pointer for function
*Return: always 0
*/
int _strlen(char *s)
{
	return (strlen(s));
}
